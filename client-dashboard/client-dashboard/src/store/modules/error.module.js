import { state, mutations } from '@/store/mutations/error.mutation'
import { actions } from '@/store/actions/error.action'

export default {
    namespaced: true,
    state,
    mutations,
    actions
}